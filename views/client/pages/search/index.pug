extends ../../layouts/default.pug
include ../../mixins/product-layout.pug

block main 
  .container.my-3 
    .row 
      .col-12 
        h1 Kết quả tìm kiếm
    
    if(products.length > 0)
       .list-products
        .container
          +product-grid(products)
    else 
      h3(class="text-center") Không tìm thấy sản phẩm